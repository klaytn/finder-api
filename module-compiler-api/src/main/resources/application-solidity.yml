finder:
  contract:
    solidity:
      compiler:
        root-path: "/tmp/solidity"
---
spring:
  config:
    activate:
      on-profile: stag,prod

finder:
  contract:
    solidity:
      compiler:
        gcs-bucket: ${GCS_PRIVATE_BUCKET}
