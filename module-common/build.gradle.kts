group = "io.klaytn.finder"

version = "1.0.0-SNAPSHOT"

tasks.jar {
    archiveBaseName.set("finder-common")
    archiveVersion.set("1.0.0-SNAPSHOT")
}

tasks.kotlinSourcesJar {
    archiveBaseName.set("finder-common")
    archiveVersion.set("1.0.0-SNAPSHOT")
}
